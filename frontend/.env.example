# Supabase Configuration
# Required for full functionality (authentication, voice synthesis, etc.)
# Get these values from your Supabase project settings

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# Note: The app will load without these variables but some features
# (authentication, voice synthesis) will not work
